{
  "name": "template-subgraph",
  "license": "UNLICENSED",
  "version": "0.0.1",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:dev": "graph deploy --product hosted-service --network <network>",
    "create-local": "graph create --node <graph-node-url> <subgraph-name>",
    "remove-local": "graph remove --node <graph-node-url> <subgraph-name>",
    "deploy-local": "graph deploy --node <graph-node-url> --ipfs <ipfs-url> <subgraph-name>",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
